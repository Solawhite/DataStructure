栈
	仅在一段进行插入和删除运算的线性表称为栈，通常把执行插入和删除的这一端称为栈顶
	另一端称为栈底，栈底是固定不变的，当表中没有元素的时候称为空栈
	
	栈为后进先出（Last in first out）的线性表，简称LIFO表
	
	栈的操作：
		1. 判断是否为空
		2. 进栈或称压栈、入栈，即把一个新的元素压入栈顶。
		3. 出栈或称弹出栈顶的元素，即删除最后进入的数据元素
		4. 取栈顶的元素，仅做读取操作，并不删除
	
	栈的存储结构：
		采用顺序存储和链式存储，分别称为顺序栈和链栈。
		
		
		顺序栈
			采用顺序存储结构存放的栈，是操作受限的顺序表，但是克服了顺序表插入和删除
			时需要搬移元素的弊端
			
			应用：表达式求解 互换 后缀表达式 中缀表达式
			
		链栈
			链式存储，操作受限的非循环单链表
			
		