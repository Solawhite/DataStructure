队列（在jdk中为接口interface全都自己实现 其中LinkedList实现了，可以直接用来实现简单队列）
	仅在一段进行插入而另一端进行删除的线性表称为队列，允许删除的一端称为队头，
	运行插入的一端称为队尾，当队列没有元素时称为空队列，FIFO表
	
	队列的操作
		1. 判断是否为空
		2. 进队列
		3. 出队列
		4. 取队头的元素，不删除
		
	存储结构
		同栈
		
		循环顺序队列
			两个指针，一个指头，一个尾。
			实际使用时，头指针只会后移，前面的空间不能使用
			如此时尾指向最后一个，则无法再入栈，但实际头部还有空间，称为“假上溢”
			
			为充分利用空间，采取循环队列。
			即在入栈出栈过程中，front和rear任然加一，在超过数组base上界时，指向0
			这种操作可以用模操作实现
			front = (front+1)%queueSize
			rear = (rear+1)%queueSize
			
			此时，空队列和满队列时，头尾指针都相等，无法通过相等判断是否空，
			可以采用少用一个存储空间来解决，约定入队前，测试队尾指针在循环意义下加一
			是否等于队头指针，相等则认为队满，而把front=rear作为队空判断条件
		非循环链队列
			两个指针分别指向链表头尾。
			